using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HandleColor : MonoBehaviour
{
    //Renderer handleColor;
    //public Color color = new Vector4(0, 0, 0, 1.0f);
    // Start is called before the first frame update
    void Start()
    {
        //handleColor = gameObject.GetComponent<Renderer>();
        //handleColor.material.color = new Color(255.0f, 255.0f, 255.0f, 0.0f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
